# Release Notes

- Hardened stale-browser recovery so `UIIntegrator` can reconnect cleanly after page-only cleanup without hanging on stale CDP close operations.
- Added regression coverage for the browser-manager reset path.
